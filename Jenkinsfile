def gv
pipeline{

    agent any
    stages{
        stage("init"){
            steps{
                sh "mvn --version"
            }
        }
        stage("build"){
            steps{

            }
        }

        stage("test"){
            steps{
            
            }
        }

        stage("deploy"){

            steps{
              
            }
        }
    }
    
}