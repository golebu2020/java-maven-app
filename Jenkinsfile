#!/usr/bin/env groovy

pipeline{
    agent any
    stages{
        stage ("increment build"){
            steps{
                script{
                    echo "increment"
                }
            }
        }
        
        stage ("build artifact"){
            steps{
                script{
                    echo "increment"
                }
            }
        }

        stage ("build image & push"){
            steps{
                script{
                    echo "Increment"
                }
            }
        }

        stage ("deploy"){
            steps{
                script{
                    echo "increment"
                }
            }
        }
    }
}